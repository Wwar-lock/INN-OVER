<p id="notice"><%= notice %></p>

<h1 align="center">List Of All Articles</h1>

<% @articles.each do |article| %>
  <div class="row">
    <div class="col-xs-8 col-xs-offset-2">
      <div class="well well-lg">
        <div class="articles-title">
          <%= link_to article.title,article_path(article) %>
        </div>
      <div class="articles-body">
        <%= truncate(article.description, length: 50) %>
      </div>
      <div class="action-articles">
        <%= link_to 'Edit', edit_article_path(article),class: "btn btn-md btn-primary" %>
        <%= link_to "Delete this article",articles_path(article), method: :delete, data: { confirm: "Are you sure to PERMANENTLY DELETE this Article??"}, class: "btn btn-md btn-danger" %>
      </div>
     </div>
    </div>
  </div>
<% end %>


<br>

<%= link_to 'New Article', new_article_path %>
